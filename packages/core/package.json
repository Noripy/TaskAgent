{
  "name": "@taskagent/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure domain logic: memo schema, LLM protocol, prompts, markdown rendering. No Cloudflare/React deps.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "build": "echo 'core is consumed as TypeScript source; nothing to build'"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.1.11"
  }
}
